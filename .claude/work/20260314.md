## Fix review warnings: reply box / signatures — `338d14f`

**Request:** Fix all 6 warnings and 2 applicable suggestions from the b9ba5b6 review.

**Changes:**
- `messages/en.json` — added `actionFailed`, `deleteFailed`, `deleteConfirmTitle/Description/Button`, `deleteCancelButton`, `draftSaveFailed`, `draftDiscardFailed` keys
- `src/components/ui/alert-dialog.tsx` — added shadcn AlertDialog component
- `src/app/(app)/settings/_actions/signatures.ts` — `deleteSignatureAction` now returns `{ success: true } | { success: false; error: string }` instead of throwing
- `src/app/(app)/settings/_components/signatures-list.tsx` — replaced `confirm()` with AlertDialog; added try/catch/finally to `handleDeleteConfirmed` with `toast.error`
- `src/app/(app)/settings/_components/signature-form.tsx` — replaced hardcoded `"Something went wrong."` with `t("actionFailed")`
- `src/app/(app)/inbox/[emailId]/_components/reply-box.tsx` — added catch block to `handleSend`; replaced hardcoded `"Failed to save draft."` with `t("draftSaveFailed")`; bumped signature picker/dismiss buttons to `min-w-9 min-h-9`
- `src/app/(app)/inbox/[emailId]/_components/email-detail-view.tsx` — `handleSend` no longer throws on failure; `handleDiscardDraft` and `handleSendDraft` wrapped in try/catch with `toast.error`; replaced hardcoded `"Failed to discard draft."` with `t("draftDiscardFailed")`
- `src/app/(app)/inbox/[emailId]/page.tsx` — added `isDraft: false` to single-email fallback thread entry

---

## Fix mobile nav warnings from drawer review — `847b434`

**Request:** Fix three warnings from mobile drawer review: most pages lack mobile nav access; DESIGN.md and CLAUDE.md are stale; drawer doesn't close on back/forward navigation.

**Changes:**
- `mobile-nav-context.tsx` — added `useEffect([pathname])` to close drawer on route changes; added universal fixed hamburger button (`fixed top-1.5 right-1.5 z-30 md:hidden`) that shows on all non-inbox pages (pages that don't embed `MobileMenuButton` in their own header)
- `DESIGN.md` — removed stale bottom tab bar references; updated App Shell code snippet, Sidebar section, and Mobile Conventions to reflect the drawer pattern
- `CLAUDE.md` — updated app shell status row and mobile layout description

---

## Replace mobile bottom tab bar with hamburger drawer — `e6a0656`

**Request:** On mobile, replace the bottom tab bar with a slide-in drawer opened by a hamburger button at the top. The drawer shows the full nav (not the thin icon-only version). The reply compose bar should always sit at the bottom of the screen on mobile.

**Plan:**
- Create a `MobileNavProvider` client component that manages open/close state and renders the drawer overlay + backdrop as fixed-position elements above all content
- Export a `MobileMenuButton` component from the same file so individual page headers can add the hamburger without prop-drilling
- Remove the mobile bottom tab bar from `sidebar.tsx`
- Remove all `pb-16 md:pb-0` space reservations (layout `main` + email detail outer div) since there's no longer a tab bar to clear
- Add `MobileMenuButton` to the conversation view header (right side) and the inbox mobile top bar
- Fix the inbox compose FAB position (`bottom-20` → `bottom-6`)

**Changes:**
- `src/app/(app)/_components/mobile-nav-context.tsx` — new file: `MobileNavProvider` (slide-in drawer with full nav + backdrop), `MobileMenuButton` (hamburger, `md:hidden`), `useMobileNav` hook
- `src/app/(app)/_components/sidebar.tsx` — removed `TabItem` component and mobile bottom `<nav>`
- `src/app/(app)/layout.tsx` — wrapped with `MobileNavProvider`; removed `pb-16 md:pb-0` from `<main>`
- `src/app/(app)/inbox/[emailId]/_components/email-detail-view.tsx` — added `MobileMenuButton` import; added to header right side; `MoreHorizontal` is `hidden md:flex` (desktop only); account pill is `hidden sm:inline-block`; removed `pb-16 md:pb-0` from outer div
- `src/app/(app)/inbox/_components/inbox-view.tsx` — added `MobileMenuButton` to mobile top bar row; FAB `bottom-20` → `bottom-6`
- `messages/en.json` — added `nav.openMenu` and `nav.closeMenu` keys

---

## Fix hydration mismatch on message timestamp — `b1726f8`

**Request:** Hydration error in the conversation view — `Intl.DateTimeFormat` produces different strings on Node.js vs browser (ICU data difference).

**Changes:**
- `email-detail-view.tsx` — added `suppressHydrationWarning` to the timestamp `<span>`

---

## Fix Intl hydration errors with client-deferred date formatting — `210179f`

**Request:** Fix three issues flagged in the hydration fix review: `suppressHydrationWarning` doesn't cover `title` attributes; `formatTime`/`formatFullDate` duplicated in detail view; `email-card.tsx` has the same latent bug.

**Changes:**
- `src/lib/email-display.ts` — added `formatTime()` and `formatFullDate()` exports
- `email-detail-view.tsx` — removed local `formatTime`/`formatFullDate`; imported from `email-display.ts`; `MessageBubble` uses `useState("")` + `useEffect` for both `time` and `fullDate`; removed `suppressHydrationWarning`
- `email-card.tsx` — added `useEffect`/`useState` imports; `formattedDate` deferred to client via `useState("")` + `useEffect`

## Redesign Gatekeeper screen with approve/block actions — `5fbeef6`

**Request:** Redo the Gatekeeper screen to match the inbox layout, with Hey.com-style large approve/deny buttons per sender entry.

**Plan:**
- Create a Server Action (`_actions/decisions.ts`) that handles four decisions: approve, block, feed, paper_trail — creates a sender rule, re-categorises all screener emails from that sender, and removes the screener queue entry.
- Rewrite `gatekeeper-list.tsx` to match the inbox layout structure (same container, padding, header style), with optimistic entry removal on decision.
- Rewrite `gatekeeper-row.tsx` to include inline action buttons: green "Approve" pill, red "Block" pill, and a "More" Popover for Feed/Paper Trail — displayed below content on mobile, inline on desktop.
- Remove the old `h-full flex-col` wrapper from `page.tsx` since the list component now owns its own layout.
- Add translation keys: `approve`, `block`, `feed`, `paperTrail`, `more`, `senderCount`.

**Changes:**
- `src/app/(app)/gatekeeper/_actions/decisions.ts` — new Server Action: upserts sender rule, re-categorises emails, deletes screener queue entry
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — rewritten as GatekeeperView: inbox-style layout container, optimistic entry removal, `useTransition` for async decision handling
- `src/app/(app)/gatekeeper/_components/gatekeeper-row.tsx` — rewritten with Approve/Block pill buttons and More Popover (Feed, Paper Trail), responsive mobile/desktop layout
- `src/app/(app)/gatekeeper/page.tsx` — removed old outer wrapper; now renders GatekeeperList directly
- `messages/en.json` — added `approve`, `block`, `feed`, `paperTrail`, `more`, `senderCount` keys under `pages.gatekeeper`

## Fix: wrap gatekeeper decision writes in a db transaction — `8ea7cb5`

**Request:** Fix Critical from review — the three sequential DB writes in the gatekeeper Server Action were not atomic.

**Plan:** Wrap the upsert sender rule + update emails + delete screener queue entry in `db.transaction(async (tx) => { ... })`, using `tx` instead of `db` for all writes inside.

**Changes:**
- `src/app/(app)/gatekeeper/_actions/decisions.ts` — wrapped three writes in `db.transaction()`

## Fix pre-existing TypeScript build errors — `fa7b307`

**Request:** Fix "bunch of errors in the app interface" — build was failing; update workflow to run existing e2e tests before committing.

**Plan:**
Three root causes:
1. BullMQ v5 bundles its own ioredis, causing type mismatch when passing a top-level `IORedis` instance as `connection`. Fix: cast `redis as any` with an explanatory comment, and add the job name string literal as the third generic parameter on `Queue<>` so BullMQ v5's stricter `ExtractNameType` constraint is satisfied.
2. Worker `addBulk` passes `name: "process-email"` inferred as `string` rather than a literal — fix with `as const`.
3. `@types/nodemailer` was not installed, so `smtp/client.ts` had implicit `any` on the import.
Also updated `CLAUDE.md` Definition of Done to include `npm run build` and `npm run test:e2e` before committing.

**Changes:**
- `src/lib/queue/index.ts` — cast `redis as any` for queueConnection; added `"sync-account"` and `"process-email"` as third type params to `Queue<>`
- `src/lib/queue/workers/sync.worker.ts` — cast `redis as any` for workerConnection; added `as const` to `"process-email"` job name in `addBulk`
- `package.json` / `package-lock.json` — added `@types/nodemailer` as dev dependency
- `CLAUDE.md` — updated Definition of Done: build + e2e tests must pass before committing

## Align Gatekeeper layout with inbox — `d60a59b`

**Request:** Gatekeeper should match inbox layout: mailbox filter at top, date group dividers, remove the coloured dot from cards, remove the dotted "More" dropdown, and move Approve/Block buttons outside the card onto the same line (card → Approve → Block), matching Hey.com's side-by-side approach.

**Plan:**
- `gatekeeper-list.tsx`: copy mailbox filter UI and date-grouping logic from `inbox-view.tsx`; add desktop and mobile top bars; use `GroupDivider`; pass `accounts` + `mailAccountId` for filtering.
- `gatekeeper-row.tsx`: strip to email-info-only card (no dot, no action buttons, no More); outer wrapper is `flex flex-col gap-2 md:flex-row md:items-stretch`; buttons go in a `<div className="flex gap-2 md:contents">` so on desktop they become direct flex siblings of the card (inheriting the row's `items-stretch`), while on mobile they render as a side-by-side row below the card.
- `page.tsx`: add `mailAccountId` to the select and a separate `allAccounts` query.

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — full rewrite: mailbox filter, date grouping, top bars, `GroupDivider`; removed `emptyMessage` prop (inline)
- `src/app/(app)/gatekeeper/_components/gatekeeper-row.tsx` — stripped to email info card + outside Approve/Block buttons using `md:contents` wrapper
- `src/app/(app)/gatekeeper/page.tsx` — added `mailAccountId` to select; added `allAccounts` parallel query; updated `GatekeeperList` props

## Fix hardcoded button labels; strengthen i18n rule — `e1cf138`

**Request:** Fix Warning from review (Approve/Block were hardcoded English strings); update workflow to enforce translations always.

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-row.tsx` — added `useTranslations("pages.gatekeeper")`, replaced `"Approve"` and `"Block"` literals with `t("approve")` and `t("block")`
- `CLAUDE.md` — strengthened i18n rule to a hard no-exceptions requirement: every user-visible string must be in `messages/en.json` before being rendered

## Rebuild Gatekeeper from Inbox as base — `c5923fd`

**Request:** Discard the existing Gatekeeper implementation and rebuild by copying the Inbox page and its components as the starting point. Apply only these changes: remove the gatekeeper alert, search, and new message button; make the title a plain heading (no dropdown); put Approve and Block buttons on the same row after each email card (same height/rounded corners as the card, card is shorter since buttons share the row width).

**Plan:**
- Copy `email-card.tsx` → `gatekeeper-card.tsx`: change `<Link>` to `<div>`, add `flex-1 min-w-0` on the root element so it stretches within the row. Type is `GatekeeperEmail` (same shape as `InboxEmail`, mapped from screener_queue).
- Copy `inbox-view.tsx` → `gatekeeper-list.tsx`: remove gatekeeper alert, `SearchCommand`, New button, mobile FAB, folder switcher (`FOLDERS`, `FolderSwitcherContent`, `ChevronDown`, folder state/navigation). Title becomes a plain `<h2>`. Each card in the list wrapped in `flex items-stretch gap-2` with Approve + Block buttons; `items-stretch` makes buttons match card height without explicit sizing. Decision state (`entries`, `pendingIds`, `useTransition`) handles optimistic removal.
- `page.tsx`: add `mailAccountName` to select, map screener_queue rows to `GatekeeperEmail` shape (sentAt = lastSeenAt, threadCount = messageCount, isRead = false, threadId = null).
- Delete `gatekeeper-row.tsx` (no longer needed).

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-card.tsx` — new: copy of email-card, `<div>` instead of `<Link>`, `flex-1 min-w-0` on root
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — rewritten from inbox-view; removed gatekeeper alert/search/FAB/folder switcher; plain title; decision row with Approve/Block buttons
- `src/app/(app)/gatekeeper/_components/gatekeeper-row.tsx` — deleted
- `src/app/(app)/gatekeeper/page.tsx` — added `mailAccountName`, mapped rows to `GatekeeperEmail`

## Fix: toast error on failed gatekeeper decision; document backlog — `7175a7e`

**Request:** Fix Warning #1 (no error feedback on failed decisions); document warnings #2 and #3 in backlog.

**Plan:**
- Install `sonner` (shadcn standard toast library); add `<Toaster theme="dark" position="bottom-right" />` to root layout.
- Wrap `makeGatekeeperDecision` in try/catch; call `toast.error(t("decisionFailed"))` on both `{ success: false }` result and unexpected throws; use `finally` to always clear the pending state regardless of outcome.
- Add `decisionFailed` translation key.
- Append warnings #2 (mobile touch targets) and #3 (GatekeeperCard/GatekeeperEmail duplication) to backlog.md.

**Changes:**
- `src/app/layout.tsx` — added `<Toaster>` from sonner
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — try/catch/finally in `handleDecision`; `toast.error` on failure
- `messages/en.json` — added `pages.gatekeeper.decisionFailed`
- `package.json` / `package-lock.json` — added `sonner`
- `.claude/work/backlog.md` — added two new backlog items

## Gatekeeper spacing, unread border, and mobile button layout — `ec0afeb`

**Request:** Three Gatekeeper UI fixes: (1) top spacing too tight vs inbox, (2) remove unread left-border indicator (all entries are unread so it's always on), (3) Approve/Block buttons should stack below the email card on mobile instead of appearing beside it. Also update DESIGN.md to require mobile UX verification beyond just responsiveness.

**Plan:**
- Desktop top bar collapses to 0 height because both flanking `<div />`s are empty and the center is `absolute` — add `h-9` to give it height matching the inbox top bar.
- Remove the conditional border logic in `GatekeeperCard`; use `border-transparent` unconditionally.
- Change row layout from `flex items-stretch gap-2` to `flex flex-col gap-2 md:flex-row md:items-stretch`. Wrap buttons in `<div className="flex gap-2 md:contents">` so on mobile they're full-width side-by-side, and on desktop `md:contents` makes the wrapper transparent so children participate directly in the parent flex and stretch to card height.
- Add mobile UX verification section to DESIGN.md.

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — added `h-9` to desktop top bar container; changed email row to `flex flex-col gap-2 md:flex-row md:items-stretch`; wrapped buttons in `flex gap-2 md:contents` div with `flex-1 md:flex-none min-h-[44px] md:min-h-0` on each button
- `src/app/(app)/gatekeeper/_components/gatekeeper-card.tsx` — removed conditional border; now always `border-transparent`
- `DESIGN.md` — added "Mobile UX verification (required)" subsection to Mobile Conventions

## Redesigned reply box with signatures & draft support — `b9ba5b6`

**Request:** Implement a structured multi-section reply box with draggable resize, signature management, and draft save/edit/send/discard lifecycle. Plan provided in detail covering 9 steps.

**Plan:**
1. DB schema: `signatures` table (userId FK, name, content, isDefault, timestamps)
2. i18n keys for signatures and draft actions
3. Signature server actions (create/update/delete) with single-default enforcement via transaction
4. Settings UI: `SignaturesList` + `SignatureForm` dialog; Settings page shows both sections
5. Draft server actions: `saveDraftAction` (upsert), `deleteDraftAction`, `sendDraftAction`
6. `ReplyBox` component: drag-to-resize, signature strip with Popover picker / dismiss, Save Draft + Send toolbar
7. `DraftBubble` component: dashed-border right-aligned bubble, Discard/Edit/Send action row
8. Update `ThreadMessage` to carry `isDraft`; `page.tsx` fetches signatures and passes them
9. Wire `ReplyBox` + `DraftBubble` into `EmailDetailView`; replace old single-line compose bar

**Changes:**
- `src/db/schema/signatures.ts` — new: signatures table with userId FK, isDefault flag
- `src/db/schema/index.ts` — added signatures named exports and double-import pattern
- `drizzle/0003_unique_wolf_cub.sql` — generated migration (applied via `db:push`)
- `messages/en.json` — added `settings.signatures`, `settings.signatureForm`, and `pages.emailDetail` draft/signature keys
- `src/app/(app)/settings/_actions/signatures.ts` — new: createSignatureAction, updateSignatureAction, deleteSignatureAction
- `src/app/(app)/settings/_components/signature-form.tsx` — new: Dialog form (name, content textarea, isDefault checkbox)
- `src/app/(app)/settings/_components/signatures-list.tsx` — new: section list with add/edit/delete, Default badge
- `src/app/(app)/settings/page.tsx` — parallel-fetched accounts + signatures; renders SignaturesList below AccountsList
- `src/app/(app)/inbox/[emailId]/_actions/draft.ts` — new: saveDraftAction (upsert), deleteDraftAction, sendDraftAction
- `src/app/(app)/inbox/[emailId]/_components/reply-box.tsx` — new: draggable reply box, signature strip, Popover picker, Save Draft + Send
- `src/app/(app)/inbox/[emailId]/_components/email-detail-view.tsx` — added DraftBubble, wired ReplyBox; removed old compose bar; ThreadMessage + isDraft
- `src/app/(app)/inbox/[emailId]/page.tsx` — added `isDraft` to thread query select; fetch + pass signatures

---

## Fix duplicate hamburger on gatekeeper and improve icon visibility — `68c39d8`

**Request:** On mobile, the hamburger button in the inbox is too small/obscure. On the Gatekeeper page, a second hamburger appears in the top-right corner.

**Plan:**
- `hasOwnButton` in `MobileNavProvider` only listed `/inbox` and `/inbox/*`. Gatekeeper embeds its own `MobileMenuButton` but wasn't in that list, so the universal fixed button rendered alongside it. Add `/gatekeeper` to `hasOwnButton`.
- Bump `MobileMenuButton` icon from `h-5 w-5` to `h-6 w-6` and change color from `text-muted-foreground` to `text-foreground` for better contrast on dark backgrounds. Apply the same change to the universal fixed button for consistency.

**Changes:**
- `src/app/(app)/_components/mobile-nav-context.tsx` — added `/gatekeeper` to `hasOwnButton`; updated both the universal button and `MobileMenuButton` to `text-foreground` and `h-6 w-6`
