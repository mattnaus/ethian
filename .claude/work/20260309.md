## NextAuth.js v5 auth flow — `(user-built, no hash)`

**Request:** Set up NextAuth.js v5 credentials-based authentication — login, register, session, and route protection.

**Plan:**
- Use NextAuth v5 `Credentials` provider with bcrypt password verification
- JWT session strategy (no DB sessions table needed)
- Protect `(app)/*` routes via middleware + server-side `auth()` check in layout
- Auth pages (`/login`, `/register`) in `(auth)` route group with shared centered layout
- Client-side forms using React 19 `useActionState` + Server Actions
- Root `/` page redirects based on session state

**Changes:**
- `src/auth.ts` — NextAuth v5 config: Credentials provider, JWT callbacks, session type
- `src/auth.d.ts` — Type augmentation to add `id` to `Session["user"]`
- `src/middleware.ts` — Route protection: unauthenticated → /login; logged-in auth pages → /imbox
- `src/app/api/auth/[...nextauth]/route.ts` — NextAuth route handler
- `src/app/(auth)/layout.tsx` — Centered card layout for auth pages
- `src/app/(auth)/_actions/auth.ts` — `loginAction` and `registerAction` Server Actions
- `src/app/(auth)/login/page.tsx` — Login page (server component)
- `src/app/(auth)/login/_components/login-form.tsx` — Login form (client, useActionState)
- `src/app/(auth)/register/page.tsx` — Register page (server component)
- `src/app/(auth)/register/_components/register-form.tsx` — Register form (client, useActionState)
- `src/app/(app)/layout.tsx` — Protected layout: redirects to /login if no session
- `src/app/(app)/imbox/page.tsx` — Placeholder Imbox page with sign-out
- `src/app/page.tsx` — Root redirect: /imbox if logged in, /login if not

## Environment and worker setup fixes — `53a6729` / `a0dac66`

**Request:** Resolve local setup blockers: PostgreSQL not installed, wrong DATABASE_URL, worker process failing to load env vars.

**Plan:**
- PostgreSQL wasn't installed; user installed it via Homebrew and corrected DATABASE_URL (was placeholder `user`, needed macOS username)
- Worker env loading: static `import` statements are hoisted before any code runs (including `dotenv.config()`), so the dotenv call in `sync.worker.ts` was ineffective. Fix by using Node's built-in `--env-file` flag which loads env before any module is executed.

**Changes:**
- `package.json` — `worker:dev` script changed from `tsx watch ...` to `node --env-file=.env.local --require tsx/cjs --watch ...`
- `src/lib/queue/workers/sync.worker.ts` — removed dotenv import and config call (no longer needed; env loaded by Node before any module runs)
- `CLAUDE.md` — updated Current Status: auth marked done, directory structure `[planned]` markers updated

## Mail account CRUD — `49b33b5` / `bff1026`

**Request:** Build settings page with mail account CRUD: add, edit, delete, list.

**Plan:**
- Add `verifyImapConnection` to IMAP client (was missing, needed by action)
- Server Actions with Zod validation, ownership checks, IMAP connection test on add
- Settings page (server component) fetches accounts; AccountsList (client) handles UI state
- AccountForm dialog with useActionState for add/edit; key prop for correct remount
- shadcn/ui components, dark zinc+orange theme

**Changes (`49b33b5`):**
- `src/lib/imap/client.ts` — added `verifyImapConnection` (connect, lock INBOX, logout)
- `src/app/(app)/settings/_actions/accounts.ts` — `addMailAccountAction`, `updateMailAccountAction`, `deleteMailAccountAction`
- `src/app/(app)/settings/page.tsx` — server component, fetches accounts
- `src/app/(app)/settings/_components/accounts-list.tsx` — client component, list + delete
- `src/app/(app)/settings/_components/account-form.tsx` — dialog form, useActionState
- `src/app/(app)/_components/sidebar.tsx` — icons + labels, orange active state
- `src/app/(app)/layout.tsx` — sidebar + main shell
- `src/app/(app)/imbox/page.tsx` — cleaned up for app shell
- `src/app/globals.css`, `src/app/layout.tsx` — dark-only theme, html.dark
- `components.json`, `src/components/ui/` — shadcn/ui setup + components

**Code review fixes (`bff1026`):**
- Double-encrypt bug: encrypt password once, reuse variable (was calling encrypt twice)
- Remove dead `verifySmtpConnection` import
- Delete confirmation before action
- Per-row pending state (was shared across all rows)
- `key` prop on AccountForm to remount on add/edit switch
- Port field error display (imapPort, smtpPort)
- Sanitize IMAP error message (log internally, show generic to user)
- `aria-label` on icon-only buttons

## Playwright e2e testing framework — `1209932`

**Request:** Set up Playwright e2e testing framework with auth tests and settings tests. IMAP tests skippable unless `TEST_IMAP_HOST` is set.

**Plan:** Separate test DB (`ethian_test`), global setup pushes schema with drizzle-kit, serial test execution, db helpers for teardown. Auth tests cover redirect, register, login flows. Settings tests cover empty state, dialog open/close, IMAP add/delete.

**Changes:**
- `playwright.config.ts` — test config: port 3001, serial, 1 worker, webServer with `DATABASE_URL` + `NEXTAUTH_URL` overrides, globalSetup
- `tests/global-setup.ts` — `drizzle-kit push --force` on test DB before suite runs
- `tests/helpers/auth.ts` — `register()` / `login()` page helpers, bypass HTML validation
- `tests/helpers/db.ts` — `deleteUser()` / `closeDb()` for test teardown
- `tests/auth.spec.ts` — 9 tests: redirects, register (success/duplicate/short-password), login (valid/wrong/unknown), logged-in redirect
- `tests/settings.spec.ts` — 4 tests (2 IMAP-skipped): empty state, dialog open/close, IMAP add success/failure
- `package.json` — `test:e2e` and `test:e2e:imap` scripts, `@playwright/test` devDependency
- `.gitignore` — `playwright-report/`, `test-results/`
- `.env.example` — documented `DATABASE_URL_TEST`, `TEST_IMAP_*`, `TEST_SMTP_*` vars

**Key fixes during implementation:**
- `drizzle-kit push` prompt: uses arrow-key navigation, not text input → fixed with `--force` flag
- `NEXTAUTH_URL` mismatch: test server on 3001 but `.env.local` has port 3000 → override in webServer `env`
- HTML `minLength` on password field blocks server-side validation test → `register()` helper strips HTML constraints before submit
- Logged-in redirect test must `await expect(page).toHaveURL("/imbox")` before navigating to `/login`

**Result:** 12/14 tests pass, 2 IMAP tests skipped (no `TEST_IMAP_HOST`)

## Playwright e2e — review fixes — `4025a61`

**Request:** Implement findings from code review `playwright-e2e-1209932.md`.

**Changes:**
- `tests/global-setup.ts` — surface stderr on drizzle-kit failure; verify `users` table exists after schema push
- `playwright.config.ts` — `trace: "retain-on-failure"`; `expect: { timeout: 10_000 }`
- `tests/auth.spec.ts` — clear cookies in `beforeEach`
- `tests/settings.spec.ts` — clear cookies in `beforeEach`; read IMAP env vars inside each test after `test.skip`
- `tests/helpers/auth.ts` — `form.noValidate = true` replaces per-input attribute removal

## UI design system — `97cbb8b`

**Request:** Define design direction before building UI.

**Plan:** Lock in design decisions in CLAUDE.md before any UI is built, so all future components are consistent.

**Changes:**
- `CLAUDE.md` — added "UI & Design System" section: principles (shadcn/ui only, dark only), color palette (zinc scale + orange-500 accent), app shell layout spec, typography, component conventions
- `CLAUDE.md` — updated Work Log Convention: entries now tagged with commit hash (first 8 chars)
