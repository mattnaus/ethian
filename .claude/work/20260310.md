## Account color picker — `9ac16b0`

**Request:** Each mail account should have a color for UI identification. When adding a new account, a random color is picked as default; the user can overwrite it by selecting from a palette.

**Plan:** Add `color` column (text, not null, default `#3b82f6`) to `mail_accounts`. Wire it through the Zod schema and both add/update Server Actions. Add a row of 8 colored swatches to the account form (random default on add, existing value on edit). Show a small colored dot in the accounts list.

**Changes:**
- `src/db/schema/accounts.ts` — added `color` column
- DB — applied `ALTER TABLE mail_accounts ADD COLUMN color text NOT NULL DEFAULT '#3b82f6'` directly
- `src/app/(app)/settings/_actions/accounts.ts` — added `color` to Zod schema, raw form data extraction, candidate object, and DB insert/update
- `src/app/(app)/settings/_components/account-form.tsx` — added `ACCOUNT_COLORS` palette, `randomColor()`, `selectedColor` state, color swatch picker UI, hidden `color` input
- `src/app/(app)/settings/_components/accounts-list.tsx` — added colored dot indicator next to account name

## Collapsible sidebar — `e921f38`

**Request:** Sidebar should toggle between thin (icon-only) and wide (icon + label) modes. Toggle button below Screener divider. Collapse button top-right when wide. Mobile always uses the wide bottom tab bar. Smooth animation.

**Plan:** Animate `aside` width with `transition-[width] duration-200 ease-in-out` between `w-14` and `w-56`. Persist state to `localStorage`. In thin mode, nav items use Tooltip wrappers; in wide mode, label is rendered inline and tooltips are dropped. Toggle button below Screener divider changes icon/label based on mode. Header shows collapse button only in wide mode. Mobile tab bar unchanged.

**Changes:**
- `src/app/(app)/_components/sidebar.tsx` — rewrote with `expanded` state, localStorage persistence, `DesktopNavItem` component handling both modes, PanelLeftOpen/PanelLeftClose toggle controls, animated width transition

## Fix Tailscale redirect + rename /imbox → /inbox — `2f012dc`

**Request:** After logging in via a Tailscale DNS name, the app redirects to localhost and errors out. Also rename the /imbox route to /inbox.

**Plan:** Add `trustHost: true` to the NextAuth config so it uses the request's Host header for redirects instead of hardcoded NEXTAUTH_URL. Rename the /imbox directory, route references, test expectations, and CLAUDE.md.

**Changes:**
- `src/auth.ts` — added `trustHost: true` to NextAuth config
- `src/app/(app)/imbox/` → `src/app/(app)/inbox/` — renamed directory
- `src/app/(auth)/_actions/auth.ts` — redirectTo: "/imbox" → "/inbox"
- `src/middleware.ts` — redirect URL /imbox → /inbox
- `src/app/page.tsx` — root redirect /imbox → /inbox
- `src/app/(app)/_components/sidebar.tsx` — nav href /imbox → /inbox
- `CLAUDE.md` — updated directory tree
- `tests/auth.spec.ts`, `tests/settings.spec.ts`, `tests/helpers/auth.ts` — all /imbox → /inbox
