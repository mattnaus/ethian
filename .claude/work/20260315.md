# Work Log — 2026-03-15

## Gatekept — Sender Rules Management Page — `945e851`

**Request:** Implement a "Gatekept" page at `/gatekept` for reviewing, changing, and deleting existing sender rules. Rename the sidebar "Gatekeeper" entry to "Gatekept" with `ShieldCheck` icon.

**Plan:**
- Create server actions for updating rule decisions (with email re-categorization) and deleting rules
- Build server page querying all sender rules for the user
- Build client list component with filter tabs (All/Approved/Feed/Paper Trail/Blocked), search, and decision-type grouping
- Build card component with decision badge (Popover for change) and delete button (AlertDialog confirmation)
- Update sidebar and mobile nav to point at `/gatekept` with `ShieldCheck` icon
- Add `/gatekept` to `hasOwnButton` in mobile nav context

**Changes:**
- `messages/en.json` — Added `nav.gatekept` and full `pages.gatekept` namespace (title, empty state, filter labels, group headers, decision labels, delete confirmation, search placeholder, badge labels)
- `src/app/(app)/gatekept/_actions/rules.ts` — Server actions: `updateSenderRuleDecision` (updates rule + re-categorizes emails in transaction) and `deleteSenderRule` (deletes rule, leaves existing emails as-is)
- `src/app/(app)/gatekept/page.tsx` — Server component: auth, queries sender_rules with count, maps to serializable shape
- `src/app/(app)/gatekept/_components/gatekept-list.tsx` — Client list: filter tabs, search, decision-type grouping (when "All"), optimistic state, handlers with try/catch/finally pattern
- `src/app/(app)/gatekept/_components/gatekept-card.tsx` — Card: avatar, sender display, applies-to badge, decision badge (Popover with options), delete (AlertDialog), relative date
- `src/app/(app)/_components/sidebar.tsx` — Changed nav item: `/gatekeeper` → `/gatekept`, `ShieldQuestion` → `ShieldCheck`, label key `gatekeeper` → `gatekept`
- `src/app/(app)/_components/mobile-nav-context.tsx` — Same nav item change + added `/gatekept` to `hasOwnButton` check

## Reviewer fixes — `0e50b7b`

**Request:** Fix Critical + Warnings from reviewer on `945e851`.

**Changes:**
- `src/app/(app)/gatekept/_actions/rules.ts` — Added `escapeLike()` to sanitize SQL wildcards in domain LIKE patterns; wrapped both server actions in try/catch to return `{ success: false }` instead of throwing
- `src/app/(app)/gatekept/_components/gatekept-list.tsx` — Replaced hardcoded "Showing X of Y" with `t("showingOf")` i18n call
- `messages/en.json` — Added `pages.gatekept.showingOf` key

## Rebuild Drafts screen — `c1a2abd`

**Request:** Remove and rebuild the Drafts screen based on the Inbox screen layout. Match styles exactly. "New" button links to compose. No gatekeeper button. Header says "Drafts" (no folder switcher dropdown). Clicking a draft opens compose with prepopulated data.

**Plan:**
- Create `DraftCard` component modeled on `EmailCard` — shows recipients instead of sender, links to `/compose?draft={id}`
- Create `DraftsView` component modeled on `InboxView` — same layout/styles minus gatekeeper button and folder switcher, "Drafts" as static heading, mailbox filter + search + "New" button
- Rewrite `page.tsx` to match inbox pattern: fetch drafts with account info, attachment check, total count
- Add translation keys: `draftCount`, `searchPlaceholder`, `showingOf`
- Register `/drafts` in mobile nav `hasOwnButton` check

**Changes:**
- `src/app/(app)/drafts/_components/draft-card.tsx` — New component: draft card with avatar (recipient initials), account color ring, recipients display, subject + snippet, attachment indicator, relative date
- `src/app/(app)/drafts/_components/drafts-view.tsx` — New component: full drafts view with desktop/mobile top bars, mailbox filter, search, date-grouped draft list, empty state, mobile FAB
- `src/app/(app)/drafts/page.tsx` — Rewritten: server component fetching drafts with attachment subquery, total count, all accounts, locale; passes to DraftsView
- `messages/en.json` — Added `pages.drafts.draftCount`, `searchPlaceholder`, `showingOf`
- `src/app/(app)/_components/mobile-nav-context.tsx` — Added `/drafts` to `hasOwnButton` check

## Standalone Draft Conversation View — `d6c3386`

**Request:** Implement a standalone draft conversation view at `/drafts/[draftId]`. Clicking a draft from the list opens this view showing the draft as a DraftBubble with Edit/Discard/Send actions. Saving a draft from compose redirects here instead of staying on the form.

**Plan:**
- Create server page fetching draft + account info with auth/UUID guards
- Create client component with header (back button, avatar, recipient, account badge), subject bar, To line, DraftBubble with Edit/Discard/Send
- Create two server actions: `discardStandaloneDraftAction` (delete draft) and `sendStandaloneDraftAction` (SMTP send + insert sent row + delete draft)
- Update `draft-card.tsx` link from `/compose?draft=` to `/drafts/[id]`
- Update `compose-view.tsx` to redirect to `/drafts/[id]` after saving a draft
- Add `pages.draftDetail` i18n namespace

**Changes:**
- `src/app/(app)/drafts/[draftId]/page.tsx` — Server component: UUID validation, auth, fetches draft + account via JOIN, passes to DraftDetailView
- `src/app/(app)/drafts/[draftId]/_components/draft-detail-view.tsx` — Client component: header with back/avatar/recipient/account badge, subject, To line, DraftBubble with Edit/Discard/Send actions
- `src/app/(app)/drafts/[draftId]/_actions/draft.ts` — Server actions: `discardStandaloneDraftAction` (ownership check + delete), `sendStandaloneDraftAction` (fetch draft + SMTP send + delete draft + insert sent row)
- `src/app/(app)/drafts/_components/draft-card.tsx` — Changed link href from `/compose?draft=` to `/drafts/`
- `src/app/(app)/compose/_components/compose-view.tsx` — After saving draft, redirect to `/drafts/{id}` instead of updating local state
- `messages/en.json` — Added `pages.draftDetail` namespace (back, noSubject, noRecipient, toLabel, draftLabel, editDraft, discardDraft, sendDraft, error messages)

## Persist Signature Choice on Drafts — `63d2520`

**Request:** Signatures are appended at send time in the UI but lost when sending from a draft bubble (standalone or in-thread). Reopening a draft for editing also resets the signature to default.

**Plan:**
- Add nullable `signatureId` FK column to `emails` table (onDelete: set null)
- Save actions store the chosen signatureId in both INSERT and UPDATE paths
- Send-from-draft actions (standalone + in-thread) fetch signature content server-side and append before SMTP send
- Server pages select signatureId and pass it through to UI components
- UI components restore the stored selection when loading a draft for editing

**Changes:**
- `src/db/schema/emails.ts` — Added `signatureId` column with FK to signatures (onDelete: set null)
- `drizzle/0004_chief_hawkeye.sql` — Migration: ADD COLUMN + FK constraint
- `src/app/(app)/compose/_actions/compose.ts` — Added `signatureId` to SaveDraftSchema, INSERT, and UPDATE
- `src/app/(app)/inbox/[emailId]/_actions/draft.ts` — Added `signatureId` to SaveDraftSchema/INSERT/UPDATE; sendDraftAction fetches + appends signature content before SMTP send
- `src/app/(app)/drafts/[draftId]/_actions/draft.ts` — sendStandaloneDraftAction fetches + appends signature content before SMTP send
- `src/app/(app)/compose/page.tsx` — Added `signatureId` to draft query SELECT + pass-through
- `src/app/(app)/inbox/[emailId]/page.tsx` — Added `signatureId` to thread messages SELECT + mapping
- `src/app/(app)/compose/_components/compose-view.tsx` — DraftData includes signatureId; init activeSignatureId from draft; pass signatureId on save
- `src/app/(app)/inbox/[emailId]/_components/reply-box.tsx` — editingDraft type includes signatureId; restore on edit; pass on save
- `src/app/(app)/inbox/[emailId]/_components/email-detail-view.tsx` — ThreadMessage type includes signatureId; pass to editingDraft on Edit click

## Reviewer fixes: signature ownership + dedupe — `7409eed`

**Request:** Fix 2 Criticals (missing ownership checks on signature save/send), 1 Warning (null signatureId not restored on edit), 1 Suggestion (duplicated fetch-and-append logic).

**Changes:**
- `src/lib/signatures.ts` — New shared helper: `appendSignatureToBody()` (fetch + append, scoped by userId) and `validateSignatureOwnership()` (returns signatureId if owned, null otherwise)
- `src/app/(app)/compose/_actions/compose.ts` — Validate signatureId ownership before storing
- `src/app/(app)/inbox/[emailId]/_actions/draft.ts` — Validate on save; use `appendSignatureToBody()` on send
- `src/app/(app)/drafts/[draftId]/_actions/draft.ts` — Use `appendSignatureToBody()` on send
- `src/app/(app)/inbox/[emailId]/_components/reply-box.tsx` — Always restore signatureId from editingDraft (including null)

## Redirect to conversation after send — `b2b0248`

**Request:** After sending from compose or standalone draft view, user gets dumped back to `/drafts` or previous page with no way to see the conversation they just started. Redirect to the conversation instead.

**Changes:**
- `src/app/(app)/compose/_components/compose-view.tsx` — After successful send, redirect to `/inbox/[sentEmailId]` instead of `router.back()`
- `src/app/(app)/drafts/[draftId]/_components/draft-detail-view.tsx` — After successful send, redirect to `/inbox/[sentEmailId]` instead of `/drafts`

## Empty sentEmailId guard — `aab18c6`

**Request:** Fix reviewer warning: `sentEmailId` can be empty string, causing 404 on redirect.

**Changes:**
- `src/app/(app)/compose/_components/compose-view.tsx` — Guard: fall back to `/inbox` when `sentEmailId` is empty

## Sent page — `f96883d`

**Request:** Implement the Sent page following Inbox/Drafts layout patterns. Sent emails were unreachable after navigating away from the post-send conversation view.

**Plan:**
- Create `SentCard` component modeled on `DraftCard` — shows recipients, links to `/inbox/[id]`
- Create `SentView` component modeled on `DraftsView` — same layout with mailbox filter, search, date grouping, "Sent" heading, compose button
- Rewrite `page.tsx` to query sent emails (`isSent: true, isDraft: false`) with attachment check and total count
- Add i18n keys: title, empty, sentCount, searchPlaceholder, showingOf, noSubject, noRecipient
- Register `/sent` in mobile nav `hasOwnButton` check

**Changes:**
- `src/app/(app)/sent/_components/sent-card.tsx` — New: sent email card with recipient avatar, account color ring, subject + snippet, attachment indicator, relative date
- `src/app/(app)/sent/_components/sent-view.tsx` — New: full sent view with desktop/mobile top bars, mailbox filter, search, date-grouped list, empty state, mobile FAB
- `src/app/(app)/sent/page.tsx` — Rewritten: server component fetching sent emails with attachment subquery, total count, all accounts, locale
- `messages/en.json` — Replaced `pages.sent.comingSoon` with full namespace (title, empty, sentCount, searchPlaceholder, showingOf, noSubject, noRecipient)
- `src/app/(app)/_components/mobile-nav-context.tsx` — Added `/sent` to `hasOwnButton` check

## Section Views, Move Actions & Gatekeeper Simplification — `c929711`

**Request:** Implement Feed and Saved section views, simplify Gatekeeper to approve/block only, add "Move to" actions for reclassifying emails, update inbox folder dropdown to Inbox/Feed/Saved, and create read-only detail views for Feed and Saved.

**Plan:**
- Phase 7: Update inbox FOLDERS dropdown to Inbox/Feed/Saved (remove gatekeeper, setAside, replyLater)
- Phase 1: Simplify GatekeeperDecision to "approved" | "blocked" only, remove dead i18n keys
- Phase 2: Create Feed list view and Saved list view following Sent/Drafts patterns
- Phase 3: Create shared ReadOnlyEmailView component for feed/saved detail pages
- Phase 4: Create moveEmailAction server action and add "Move to" dropdown to inbox detail and read-only detail views

**Changes:**
- `src/app/(app)/inbox/_components/inbox-view.tsx` — Simplified FOLDERS to inbox/feed/saved, removed unused Lucide icon imports, updated getFolderLabel union type
- `src/app/(app)/gatekeeper/_actions/decisions.ts` — Narrowed GatekeeperDecision to "approved" | "blocked", removed feed/paper_trail from decisionToCategory
- `messages/en.json` — Updated inbox folders, removed gatekeeper feed/paperTrail/more keys, added full pages.feed and pages.saved namespaces, added pages.readOnlyDetail and pages.moveTo namespaces, added nav.feed
- `src/app/(app)/_components/sidebar.tsx` — Added Feed nav item (Newspaper icon) between Inbox and Drafts
- `src/app/(app)/_components/mobile-nav-context.tsx` — Added Feed nav item, added /feed and /saved to hasOwnButton check
- `src/app/(app)/feed/page.tsx` — Server component querying category "feed" emails with attachment subquery, total count, accounts
- `src/app/(app)/feed/_components/feed-view.tsx` — Client view with mailbox filter, search, date grouping, compose button
- `src/app/(app)/feed/_components/feed-card.tsx` — Card showing sender, subject, snippet, date; links to /feed/[emailId]
- `src/app/(app)/saved/page.tsx` — Rewritten from placeholder to full server component querying category "paper_trail"
- `src/app/(app)/saved/_components/saved-view.tsx` — Client view with mailbox filter, search, date grouping, compose button
- `src/app/(app)/saved/_components/saved-card.tsx` — Card showing sender, subject, snippet, date; links to /saved/[emailId]
- `src/app/(app)/_components/read-only-email-view.tsx` — Shared read-only email view with header (back, avatar, sender, account badge), subject/meta bar, HTML/text body rendering, attachments, MoveToMenu
- `src/app/(app)/feed/[emailId]/page.tsx` — Server component fetching email + attachments, renders ReadOnlyEmailView
- `src/app/(app)/saved/[emailId]/page.tsx` — Server component fetching email + attachments, renders ReadOnlyEmailView
- `src/app/(app)/_actions/move-email.ts` — Server action: validates ownership, updates email category, optionally upserts sender rule, revalidates paths
- `src/app/(app)/inbox/[emailId]/_components/email-detail-view.tsx` — Added InboxMoveToMenu component (move to feed/saved/trash with confirm step for future emails), replaced MoreOptions button

## Reviewer fixes — `66831f1`

**Request:** Fix 2 Criticals, 3 Warnings, and 1 Suggestion from reviewer on `c929711`.

**Changes:**
- `src/app/(app)/_components/read-only-email-view.tsx` — Added `isomorphic-dompurify` import, sanitize HTML with `DOMPurify.sanitize()` before `dangerouslySetInnerHTML`; added `isPending` state to MoveToMenu button with disabled/opacity feedback
- `src/app/(app)/_actions/move-email.ts` — Wrapped `db.transaction()` call in try/catch, returns `{ success: false }` on exception
- `src/app/(app)/inbox/[emailId]/_components/email-detail-view.tsx` — Added `useTransition` import; InboxMoveToMenu now uses `startTransition` instead of `void (async)()`, added `isPending` disabled/opacity feedback on trigger button
- `src/app/(app)/feed/[emailId]/page.tsx` — Removed duplicate `if (!row) notFound()` dead code check
- `src/app/(app)/_components/section-list-view.tsx` — New shared generic `SectionListView<T>` component extracting ~200 lines of duplicated boilerplate (date grouping, mailbox filter, search, top bars, list container, empty state, footer, mobile FAB)
- `src/app/(app)/feed/_components/feed-view.tsx` — Rewritten to use `SectionListView` with `renderCard` and `searchFilter` callbacks (~40 lines down from ~200)
- `src/app/(app)/saved/_components/saved-view.tsx` — Same rewrite using `SectionListView`
- `package.json` / `package-lock.json` — Added `isomorphic-dompurify` dependency

## Snooze & Trash views — `68a617a`

**Request:** Implement Phase 5 (Snooze) and Phase 6 (Trash) from the section views plan.

**Plan:**
- Phase 5: Add snoozedUntil column, snooze/unsnooze actions, snoozed list view, snooze picker UI, un-snooze worker, filter snoozed emails from section queries
- Phase 6: Create trash list view with restore, trash detail view

**Changes:**
- `src/db/schema/emails.ts` — Added nullable `snoozedUntil` timestamp column
- `drizzle/0005_nostalgic_green_goblin.sql` — Migration: ADD COLUMN snoozed_until
- `src/app/(app)/_actions/snooze-email.ts` — Server actions: `snoozeEmailAction` (sets snoozedUntil) and `unsnoozeEmailAction` (clears snoozedUntil), both with ownership validation and try/catch
- `src/app/(app)/inbox/page.tsx` — Added `isNull(emails.snoozedUntil)` to WHERE clause
- `src/app/(app)/feed/page.tsx` — Same snooze filter
- `src/app/(app)/saved/page.tsx` — Same snooze filter
- `src/app/(app)/snoozed/page.tsx` — Rewritten: server component querying `isNotNull(emails.snoozedUntil)`, ordered by snoozedUntil DESC
- `src/app/(app)/snoozed/_components/snoozed-view.tsx` — List view with mailbox filter, search, unsnooze per card (optimistic removal)
- `src/app/(app)/snoozed/_components/snoozed-card.tsx` — Card with wake-up time, unsnooze button, links to original section detail
- `src/app/(app)/_components/read-only-email-view.tsx` — Added SnoozeMenu component (presets: later today, tomorrow morning, next week) in header
- `src/app/(app)/trash/page.tsx` — Rewritten: server component querying `category: "trash"`
- `src/app/(app)/trash/_components/trash-view.tsx` — List view using SectionListView, no compose button
- `src/app/(app)/trash/_components/trash-card.tsx` — Card with restore button (moves to inbox via moveEmailAction)
- `src/app/(app)/trash/[emailId]/page.tsx` — Detail page using ReadOnlyEmailView
- `src/app/(app)/_components/mobile-nav-context.tsx` — Added /snoozed and /trash to hasOwnButton
- `src/lib/queue/workers/sync.worker.ts` — Added un-snooze checker: setInterval every 60s clears expired snoozedUntil values, runs on startup, cleared on shutdown
- `messages/en.json` — Replaced snoozed/trash comingSoon with full namespaces, added snooze picker keys

## Reviewer fixes (snooze/trash) — `6f09cd8`

**Request:** Fix 1 Critical, 5 Warnings, 3 Suggestions from reviewer on `68a617a`.

**Changes:**
- `src/app/(app)/_actions/snooze-email.ts` — Validate snooze date is valid (not NaN) and in the future before saving
- `src/app/(app)/snoozed/_components/snoozed-card.tsx` — Added finally block to handleUnsnooze
- `src/app/(app)/_components/read-only-email-view.tsx` — Added finally blocks to SnoozeMenu.handleSnooze and MoveToMenu.handleMove; memoized snooze presets with useMemo; added useMemo import
- `src/app/(app)/snoozed/_components/snoozed-view.tsx` — Rewritten to use SectionListView (was duplicating ~200 lines of boilerplate)
- `src/lib/queue/workers/sync.worker.ts` — Fixed un-snooze checker: use `.returning({ id })` instead of unreliable Array.isArray check
- `src/app/(app)/trash/[emailId]/page.tsx` — Added `eq(emails.category, "trash")` to WHERE clause
- `src/app/(app)/trash/_components/trash-card.tsx` — Changed restore button from emerald to primary accent color
- `src/db/schema/emails.ts` — Added index `idx_emails_snoozed_until` on snoozedUntil column
- `drizzle/0006_brainy_arclight.sql` — Migration: CREATE INDEX

## Gatekeeper expandable card preview — `cdc66af`

**Request:** Add a way to preview the actual email body in the Gatekeeper screen without leaving the triage flow.

**Plan:**
- Add expandable card: click a gatekeeper card to expand it inline and show the email body
- Create `fetchGatekeeperPreview` server action to fetch `bodyHtml`/`bodyText` on demand (avoid sending all bodies for 100 entries on page load)
- Cache fetched bodies client-side in a ref Map to avoid re-fetching
- Render HTML sanitized with DOMPurify, or plain text fallback, with max-height scroll
- Add chevron indicator and border highlight for expanded state

**Changes:**
- `src/app/(app)/gatekeeper/_actions/decisions.ts` — Added `fetchGatekeeperPreview` server action: joins screenerQueue → emails, returns bodyHtml/bodyText with ownership check
- `src/app/(app)/gatekeeper/_components/gatekeeper-card.tsx` — Made card clickable with expanded state: shows sanitized email body below card content, chevron rotate indicator, loading/error/empty states
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — Added `expandedId` state (one at a time), preview cache ref, `toggleExpand` callback with fetch + cache logic; passes new props to GatekeeperCard
- `messages/en.json` — Added `previewLoading`, `previewError`, `previewNoBody` keys to `pages.gatekeeper`

## Reviewer fixes (gatekeeper preview) — `be17928`

**Request:** Fix 3 Warnings from reviewer on `cdc66af`.

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — Replaced shared `previewLoading`/`previewError` booleans with `loadingId`/`errorId` strings to prevent state bleed on rapid card switching; added `toast.error()` on preview fetch failure
- `src/app/(app)/gatekeeper/_components/gatekeeper-card.tsx` — Added restrictive DOMPurify config: `FORBID_TAGS` (form, style, meta, script, iframe, object, embed) and `FORBID_ATTR` (action, formaction, http-equiv, xlink:href)

## Fix: gatekeeper button height on expand — `7ff871d`, `0254e5c`

**Request:** Approve/block buttons stayed at original stretched height when email card expanded, then shrunk too small after first fix attempt.

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — First attempt (`7ff871d`): switched to `md:items-start` when expanded, but caused buttons to shrink due to `md:min-h-0`. Final fix (`0254e5c`): reverted to always `md:items-stretch`; conditionally switch button wrapper from `md:contents` (collapsed) to `md:flex-col md:shrink-0` (expanded) so buttons keep natural height in their own container

## Fix: move buttons inside expanded card — `524005f`

**Request:** Previous fix still left buttons floating at top right, disconnected from the expanded content. Move them inside the card.

**Changes:**
- `src/app/(app)/gatekeeper/_components/gatekeeper-card.tsx` — Added `actions?: React.ReactNode` prop; renders at bottom of expanded preview area
- `src/app/(app)/gatekeeper/_components/gatekeeper-list.tsx` — When expanded: pass approve/block as `actions` into card, hide external buttons; when collapsed: keep external buttons with `md:contents` stretch layout
