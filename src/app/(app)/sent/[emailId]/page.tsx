// Re-export the inbox email detail page.
// This route exists so the sidebar highlights "Sent" instead of "Inbox"
// when viewing a sent email opened from the /sent list.
export { default } from "@/app/(app)/inbox/[emailId]/page";
